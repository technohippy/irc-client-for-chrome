/* global describe, it */

(function () {
    'use strict';

//    var assert = require('assert');
    describe('IRC.Util', function () {
        describe('#toColorClass(str)', function () {
            it('should return a class name', function () {
//                assert.equal('color', IRC.Util.toColorClass('ando'));
            });
        });
    });
})();

